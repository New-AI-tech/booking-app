feat: Implement comprehensive fixes and improvements for production readiness

This commit addresses multiple critical issues to make the booking-app repository production-ready, stable, and maintainable.

- **Backend Unification (Firebase Migration):**
  - Migrated StaffDashboard and IncomeStatement from Supabase to Firebase.
  - Introduced new Firebase services for booking and income data.
  - Created a placeholder service for maintenance logs.

- **Security Enhancements:**
  - Updated `firestore.rules` to include all relevant collections (`inventory_items`, `reservations`, `users`).

- **Environment Configuration:**
  - Aligned `src/config/env.ts` with Firebase environment variables.
  - Updated `.env.example` with all necessary environment variables.
  - Created a `.env` file with placeholder variables for testing.

- **UI and Routing Improvements:**
  - Refactored `App.tsx` to use `react-router-dom` for navigation.
  - Integrated `AdminDashboard` and `StaffDashboard` into the main application flow.

- **Code Quality and Bug Fixes:**
  - Fixed `AIService.ts` to correctly handle `AbortSignal`.
  - Resolved type mismatches in `StaffDashboard.tsx`.
  - Optimized Firestore queries in `AvailabilityChecker.tsx`.
  - Corrected relative import paths in several components.
  - Refined types to remove `any` usage.

- **Testing:**
  - Ensured tests pass by providing necessary environment variables.

This comprehensive set of changes significantly improves the application's stability, security, and maintainability, paving the way for a robust production deployment.
